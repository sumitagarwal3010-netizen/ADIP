apiVersion: apps/v1
kind: Deployment
metadata:
  name: adip-backend
  namespace: adip
  labels: { app.kubernetes.io/name: adip-backend, app.kubernetes.io/part-of: adip }
spec:
  replicas: 2
  selector:
    matchLabels: { app.kubernetes.io/name: adip-backend }
  template:
    metadata:
      labels: { app.kubernetes.io/name: adip-backend }
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/metrics"
        prometheus.io/port: "8000"
    spec:
      containers:
        - name: backend
          image: adip-backend:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          env:
            - name: ENVIRONMENT
              value: "production"
            - name: DEBUG
              value: "false"
            - name: RATE_LIMIT_ENABLED
              value: "true"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef: { name: adip-secrets, key: database-url }
          # SRE probes (Role 10): startup, readiness, liveness.
          startupProbe:
            httpGet: { path: /health, port: 8000 }
            failureThreshold: 30
            periodSeconds: 2
          readinessProbe:
            httpGet: { path: /api/v1/health, port: 8000 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /health, port: 8000 }
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: "250m", memory: "256Mi" }
            limits: { cpu: "1", memory: "512Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: adip-backend
  namespace: adip
  labels: { app.kubernetes.io/name: adip-backend }
spec:
  selector: { app.kubernetes.io/name: adip-backend }
  ports:
    - port: 80
      targetPort: 8000
      name: http
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: adip-backend
  namespace: adip
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: adip-backend
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 70 }

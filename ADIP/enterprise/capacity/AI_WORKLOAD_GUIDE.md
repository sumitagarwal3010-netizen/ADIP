# AI Workload Guide

Independent estimates per AI workload type.

## Workloads

Prompt execution, replay, regression, artifact generation, embeddings, hallucination detection, semantic similarity, rule engine, traceability, connector AI.

## Outputs

CPU (millicores), RAM (MB), latency p50, storage MB/day per workload.

## Module

`enterprise_planners.py` — `estimate_ai_workloads()`
